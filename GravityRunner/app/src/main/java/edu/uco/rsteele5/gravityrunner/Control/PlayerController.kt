package edu.uco.rsteele5.gravityrunner.Control

import android.graphics.Bitmap

class PlayerController(image: Bitmap, screenWidth: Float, screenHeight: Float) {
    var screenCenter = Pair()

    init {
        
    }
}